# ASP.NET
# Build and test ASP.NET projects.
# Add steps that publish symbols, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/aspnet/build-aspnet-4

trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

stages:
- stage: Build
  displayName: 'Build Stage'
  jobs:
  - job: Build
    displayName: 'Build Job'
    steps:
    - checkout: self

    - task: NuGetToolInstaller@1

    - task: NuGetCommand@2
      inputs:
        restoreSolution: '$(solution)'

    - task: VSBuild@1
      inputs:
        solution: '$(solution)'
        msbuildArgs: '/p:DeployOnBuild=true /p:WebPublishMethod=Package /p:PackageAsSingleFile=true /p:SkipInvalidConfigurations=true /p:PackageLocation="$(build.artifactStagingDirectory)"'
        platform: '$(buildPlatform)'
        configuration: '$(buildConfiguration)'
    - task: PublishBuildArtifacts@1

- stage: Test
  displayName: 'Test Stage'
  dependsOn: Build
  jobs:
  - job: Test
    displayName: 'Test Job'
    steps:
    - task: VSTest@2
      inputs:
        platform: '$(buildPlatform)'
        configuration: '$(buildConfiguration)'

- stage: Deploy
  displayName: 'Deploy Stage'
  dependsOn: Test
  jobs:
  - job: Deploy
    displayName: 'Deploy Job'
    steps:
      - task: CmdLine@2
        inputs:
          script: 'dir "$(Build.ArtifactStagingDirectory)" /b'



      # - task: AzureFunctionApp@2
      #   inputs:
      #     connectedServiceNameARM: 'Azure subscription 1(ae066153-bd3c-4b67-a2c8-2d4d8ea1ce38)'
      #     appType: 'functionApp'
      #     appName: 'ProjectTasksFunction20240401142019'
      #     package: 'ProjectTasksFunction/Properties/serviceDependencies.ProjectTasksFunction20240401142019 - Zip Deploy.json'
      #     deploymentMethod: 'auto'

      # - task: CmdLine@2
      #   inputs:
      #     script: |
      #       echo "Structure of work folder of this pipeline:"
      #       tree $(Agent.WorkFolder)\1 /f
            
      #       echo "Build.ArtifactStagingDirectory:" 
            
      #       echo "$(Build.ArtifactStagingDirectory)"
            
      #       echo "Build.BinariesDirectory:" 
            
      #       echo "$(Build.BinariesDirectory)"
            
      #       echo "Build.SourcesDirectory:"
            
      #       echo "$(Build.SourcesDirectory)"
      # - task: AzureWebApp@1
      #   displayName: 'Deploy to ProjecTasksCosmos'
      #   inputs:
      #     azureSubscription: 'Azure subscription 1(ae066153-bd3c-4b67-a2c8-2d4d8ea1ce38)'
      #     appType: 'webApp'
      #     appName: 'ProjectTaskCosmos20240329132020'
      #     package: '$(build.artifactStagingDirectory)/drop/**/*.zip'
      #     deploymentMethod: 'auto'

